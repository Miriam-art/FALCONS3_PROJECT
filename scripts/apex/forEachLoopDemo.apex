list < integer > iList = new list <integer>{12,3,34,5,6,7,8,9,0};
for(integer i : iList){
    system.debug(i);

}
list <string> jList = new list<string>{'Mobile Apex'};
for(string apex : jList){
system.debug(apex.toUpperCase());
}

